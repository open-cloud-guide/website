{
  "name": "example",
  "private": true,
  "version": "1.28.0",
  "license": "Apache 2.0",
  "scripts": {
    "dev": "gatsby develop -H 0.0.0.0",
    "dev:clean": "gatsby clean && gatsby develop -H 0.0.0.0",
    "build": "yarn clean && gatsby build",
    "build:clean": "yarn clean && gatsby build",
    "build:prefix": "yarn clean && gatsby build --prefix-paths",
    "serve": "gatsby serve",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "gatsby": "^2.24.14",
    "gatsby-theme-carbon": "^1.28.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
